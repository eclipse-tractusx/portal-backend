namespace CatenaX.NetworkServices.Administration.Service.Enums
{
    public enum StatusErrorCode : int
    {
        BAD_REQUEST = 400,
        NOT_FOUND = 403,
        SERVER_ERROR = 500,
        OK = 200
    }
}