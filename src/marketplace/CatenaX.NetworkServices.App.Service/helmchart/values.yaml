# Default values for container-blueprint.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
fullnameOverride: app-service
image:
  repository: test
  tag: stable
  pullPolicy: Always
  
ingress:
  serviceHost: test.com
  clusterIssuer: place
imagePullSecret: someSecret

environment: dev003

deployment:
  containerPort: 443